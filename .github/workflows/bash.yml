name: Bash validation

on:
  push:
    branches:
      - master
      - 'agent/**'
    paths:
      - '**.sh'
      - '.github/workflows/bash.yml'
  pull_request:
    paths:
      - '**.sh'
      - '.github/workflows/bash.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bash-syntax:
    name: Bash syntax
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Parse Bash scripts
        run: bash -n claude-project-mover.sh
